<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lorreneogbonna.shavit">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".views.activities.LoginActivity"
            android:label="Shavit Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.activities.CadastroActivity" />
        <activity android:name=".views.activities.PedidoClinicaActivity" />
        <activity android:name=".views.activities.HomeBuscarClienteActivity" />
        <activity android:name=".views.activities.DetalhesServicoBuscaActivity" />

        <activity android:name=".views.activities.HomeAgendamentoClinicaActivity"
                  android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".views.activities.AgendamentoActivity"
                  android:label="Agendamento">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.HomeAgendamentoClinicaActivity" />
        </activity>

        <activity android:name=".views.activities.ServicoActivity"
            android:label="Serviço">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.HomeAgendamentoClinicaActivity" />
        </activity>

        <activity
            android:name=".views.activities.PedidoAvaliacaoActivity"
            android:label="Pedido">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.HomeAgendamentoClinicaActivity" />
        </activity>

    </application>

</manifest>